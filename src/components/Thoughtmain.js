import React from 'react';
import styled from 'styled-components';
import Thought from './Thoughts';

const Mainth = styled.div`

`

export default function ThoughtMain() {
    return(
        <Mainth>
        <Thought />
        </Mainth>
    );
}